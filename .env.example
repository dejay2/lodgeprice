# Supabase Configuration
# Required environment variables for the Lodgeprice application
# Copy this file to .env and replace placeholder values with your actual credentials

# Your Supabase project URL
# Format: https://your-project-id.supabase.co
# Get this from: https://supabase.com/dashboard → Your Project → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anonymous key
# This key is safe for client-side usage when Row Level Security (RLS) is enabled
# Get this from: https://supabase.com/dashboard → Your Project → Settings → API → Project API keys
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Optional: Service role key for admin operations (only if needed)
# WARNING: Service role key bypasses RLS and should only be used server-side
# This is optional and only used for specific admin operations
VITE_SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Project-specific values for Lodgeprice:
# VITE_SUPABASE_URL=https://vehonbnvzcgcticpfsox.supabase.co
# VITE_SUPABASE_ANON_KEY=<your_actual_anon_key>
# VITE_SUPABASE_SERVICE_ROLE_KEY=<your_actual_service_role_key>

# Security Notes:
# - The anonymous key is safe for client-side usage when RLS policies are properly configured
# - Row Level Security (RLS) is enabled on all tables to control data access
# - Service role key should be treated as highly sensitive and only used when necessary
# - Never commit actual credentials to version control

# Logging Configuration
# Log levels: SILLY, TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF
# Development default: DEBUG (verbose logging for development)
# Production default: WARN (only warnings and errors)
VITE_LOG_LEVEL=DEBUG

# Performance monitoring configuration
# Enable/disable performance monitoring for database operations and UI rendering
VITE_ENABLE_PERFORMANCE_MONITORING=true

# Development mode - enables detailed debugging features
# Set to 'true' for development, 'false' for production
VITE_DEVELOPMENT_MODE=true

# Setup Instructions:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project (vehonbnvzcgcticpfsox for Lodgeprice)
# 3. Navigate to Settings > API
# 4. Copy the URL and anon key to this file
# 5. Rename this file to .env
# 6. Restart your development server
# Staging Environment Variables for Lodgeprice 2.0
# These variables should be configured for staging/preview deployments
# Used for testing before production release

# Supabase Configuration (REQUIRED)
# Can use the same Supabase project as production or a separate staging instance
VITE_SUPABASE_URL=https://vehonbnvzcgcticpfsox.supabase.co
VITE_SUPABASE_ANON_KEY=your_staging_anon_key_here

# Logging Configuration for Staging
# Staging uses INFO level for more detailed logs than production
VITE_LOG_LEVEL=INFO

# Performance Monitoring
# Enable for staging to test performance before production
VITE_ENABLE_PERFORMANCE_MONITORING=true

# Development Mode Flag
# Set to false for staging to mimic production behavior
VITE_DEVELOPMENT_MODE=false

# Staging-Specific Configuration
# Optional: Add staging-specific feature flags or settings
# VITE_ENABLE_PREVIEW_FEATURES=true
# VITE_STAGING_BANNER=true

# Deployment Notes for Staging:
#
# Vercel Preview Deployments:
# - Automatically created for pull requests
# - Use "Preview" environment variables
# - Unique URL for each deployment
#
# Netlify Deploy Previews:
# - Created for pull requests automatically
# - Use context.deploy-preview in netlify.toml
# - Accessible via unique preview URL
#
# Branch Deployments:
# - Can create staging branch for persistent staging environment
# - Configure branch-specific environment variables
# - Useful for QA and stakeholder review
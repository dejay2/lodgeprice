# Production Environment Variables for Lodgeprice 2.0
# These variables should be configured in your deployment platform's dashboard
# DO NOT commit actual values to version control

# Supabase Configuration (REQUIRED)
# These are public keys designed for client-side usage with Row Level Security
VITE_SUPABASE_URL=https://vehonbnvzcgcticpfsox.supabase.co
VITE_SUPABASE_ANON_KEY=your_production_anon_key_here

# Logging Configuration for Production
# Production should use WARN or ERROR to minimize console output
VITE_LOG_LEVEL=WARN

# Performance Monitoring
# Enable for production to track performance metrics
VITE_ENABLE_PERFORMANCE_MONITORING=true

# Development Mode Flag
# MUST be false for production deployments
VITE_DEVELOPMENT_MODE=false

# Optional: Analytics Configuration (if needed in future)
# VITE_ANALYTICS_ID=your_analytics_id_here

# Deployment Platform Notes:
# 
# For Vercel:
# 1. Go to Project Settings → Environment Variables
# 2. Add each variable without quotes
# 3. Select "Production" environment
# 4. Save and redeploy
#
# For Netlify:
# 1. Go to Site Settings → Environment Variables
# 2. Add each variable under "Production" context
# 3. Values are automatically encrypted
# 4. Trigger a new deployment

# Security Reminders:
# - The anon key is safe for client exposure (designed for public access)
# - Row Level Security (RLS) provides the actual data protection
# - Never expose service role keys in client-side code
# - All sensitive operations should use Supabase RLS policies